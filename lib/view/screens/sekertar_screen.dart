import 'package:flutter/material.dart';

class SekertarScreen extends StatelessWidget {
  const SekertarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //appBar: AppBar(),
      body: Center(
        child: Text("مرحبا بك"),
      ),
    );
  }
}
